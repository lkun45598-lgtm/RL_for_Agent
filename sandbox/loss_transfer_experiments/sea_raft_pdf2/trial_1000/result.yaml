failure_category: null
patch_spec:
  code: "import torch\ndef sandbox_loss(pred, target, mask=None, **kwargs):\n    return\
    \ ((pred-target)**2).mean()"
  mode: agent_generate
  name: probe
  strategy: faithful
timestamp: '2026-03-24T14:30:02.487954'
trial_id: 1000
validation:
  formula_interface:
    detail: 'formula requires additional model-provided loss inputs not generated
      by the current loss-only transfer pipeline: log_b, weight. BaseTrainer can forward
      model-provided loss_inputs, but the current loss-only transfer pipeline does
      not create these model outputs automatically: log_b, weight'
    error: formula_interface_incompatible
    fix_hint: '将模型改为返回 {"pred": ..., "loss_inputs": {...}}，或将论文 loss 改写为仅依赖 pred/target/mask/params
      的形式'
    passed: false
